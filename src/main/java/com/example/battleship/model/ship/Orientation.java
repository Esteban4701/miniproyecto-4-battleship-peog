package com.example.battleship.model.ship;

/**
 * Orientation in which a ship can be laid out on the board.
 */
public enum Orientation {

    /** The ship extends along increasing columns, on a single row. */
    HORIZONTAL,

    /** The ship extends along increasing rows, on a single column. */
    VERTICAL
}
