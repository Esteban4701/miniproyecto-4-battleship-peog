package com.example.battleship.view;

/**
 * Orientation in which a ship can be placed on the board.
 * <p>
 * HORIZONTAL: the ship extends along the X axis (columns).
 * VERTICAL: the ship extends along the Z axis (rows).
 * </p>
 */
public enum Orientation {
    HORIZONTAL,
    VERTICAL
}
